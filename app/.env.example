# Domain the app is deployed on. Used to build the AT Proto OAuth `clientId`
# for production and to generate public/client-metadata.json at build time.
# Copy this file to .env and adjust if deploying to a different domain.
VITE_APP_DOMAIN=beaglesteps.social
